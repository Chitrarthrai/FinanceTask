# FinanceTask Backend

This project uses **Supabase** as the backend service.

## Structure

- `schema.sql`: Contains the PostgreSQL database schema, including tables, RLS policies, and triggers.

## Deployment

The schema is deployed to the linked Supabase project.

To manage database changes, use the Supabase Dashboard or CLI.
